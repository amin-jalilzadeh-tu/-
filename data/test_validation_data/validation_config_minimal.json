{
  "description": "Minimal configuration - validates everything with defaults",
  "validation": {
    "perform_validation": true,
    "config": {
      "real_data_path": "test_validation_data/measured_data_simple.csv"
    }
  }
}