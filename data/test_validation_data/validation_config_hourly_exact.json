{
  "description": "For hourly data with exact parsed format",
  "validation": {
    "perform_validation": true,
    "config": {
      "real_data_path": "test_validation_data/measured_data_hourly_exact_format.csv",
      "aggregation": {
        "target_frequency": "daily"
      }
    }
  }
}